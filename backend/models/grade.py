class grade:
    def __init__(self, user_id, course, components) -> None:
        self.user_id = user_id
        self.course = course
        self.components = components

        """I want to calculate over grade, how many quizzes are there
        components = {quizzes: [5, 25, 2]}"""
        def calculate_grade(self):
            
